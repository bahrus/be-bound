const reBetweenBindingStatement = [
    {
        regExp: new RegExp(String.raw ``),
        defaultVals: {}
    }
];
export function prsBetween(self) {
    const { Between } = self;
    const bindingRules = [];
    return bindingRules;
}
